# claude-relay

TUI, CLI, library, and skill for the local Claude Code relay.

Install: `uv tool install claude-relay` or `pipx install claude-relay`
Bootstrap: `claude-relay init`
Launch TUI: `claude-relay tui`
